# Calamares Source

This directory contains the source-of-truth installer configuration for Phase 2.

The active installer files are delivered to the live system through the `obsidian-calamares-settings` Debian package.
