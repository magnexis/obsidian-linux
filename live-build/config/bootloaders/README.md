# Bootloaders

Phase 2 relies on Debian Live Build bootloader generation rather than a custom boot theme.

This directory is reserved for future GRUB and BIOS bootloader overrides once the base ISO workflow is verified.
